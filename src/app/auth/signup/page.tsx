import { SignupForm } from "@/components/auth/signup-form";

export const metadata = {
  title: "Sign Up - Movie Portal",
  description: "Create a new Movie Portal account",
};

export default function SignupPage() {
  return <SignupForm />;
}
