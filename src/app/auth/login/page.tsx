import { LoginForm } from "@/components/auth/login-form";

export const metadata = {
  title: "Sign In - Movie Portal",
  description: "Sign in to your Movie Portal account",
};

export default function LoginPage() {
  return <LoginForm />;
}
